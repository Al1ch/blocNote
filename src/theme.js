import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        contrastText: "#ffffff",
        main: '#0000CD',
      },
      secondary: {
        main: '#edf2ff',
      },
    },
  });


 export default theme